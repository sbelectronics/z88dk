#
# Target configuration file for z88dk
#

INCLUDE tools.inc

# Asm file which contains the startup code (without suffix)
CRT0		 DESTDIR/lib/target/isis/classic/isis_crt0

# Any default options you want - these are options to zcc which are fed
# through to compiler, assembler etc as necessary
OPTIONS		 -O2 -SO2 -iquote. -lndos -D__8080__ -D__8080 -D__ISIS__ -M -subtype=default -clib=default -m8080

CLIB      default -Cc-standard-escape-chars -m8080 -lndos -l8080_opt -lisis_clib -startuplib=8080_crt0 -pragma-define:CLIB_DISABLE_FGETS_CURSOR=1 -LDESTDIR/lib/clibs/8080

SUBTYPE   default -Cz+hex

SUBTYPE     none 
SUBTYPE		default -Cz+isis
SUBTYPE		hex -Cz+hex

INCLUDE alias.inc
